apiVersion: 1
datasources:
  - name: Prometheus
    # Fixed uid: the alert rules refer to the datasource by it.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
