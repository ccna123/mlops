# Plan 5a — API layer Implementation Plan

> **Một phần của plan này đã bị gỡ (2026-09-22):** mọi thứ liên quan tới xem
> log — `AirflowClient.get_logs` (Task 3), `filter_log_lines` và endpoint
> `GET /pipeline/runs/{run_id}/logs` (Task 5) — không còn trong code. Plan là
> biên bản việc đã làm nên giữ nguyên; **đừng dựng lại theo nó**.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dựng `services/api/` — 11 endpoint FastAPI đứng giữa dashboard và Airflow/MLflow/MinIO — rồi kết thúc bằng một UI design brief trích từ phản hồi JSON thật, đủ để Claude design dựng giao diện mà không phải đoán.

**Architecture:** Mọi collaborator được inject vào `create_app()` như `services/serving/` đang làm, nên route test được bằng pytest không cần hạ tầng. `clients/` biết Airflow/MLflow/MinIO trả về gì; `routes/` biết dashboard cần thấy gì. Dashboard **chỉ** nói chuyện với lớp này — không bao giờ gọi thẳng Airflow, MLflow hay MinIO.

**Tech Stack:** Python 3.12 (container) / 3.13 (dev), FastAPI + uvicorn, httpx, MLflow 2.22, pandas 2.3, MinIO, Airflow 2.10.3 REST API v1.

**Spec:** `docs/superpowers/specs/2026-09-20-plan5a-api-design.md` — đọc cùng plan này. Plan lập luận từ spec; hai bên lệch nhau thì spec thắng, và **phải báo chứ không tự chọn bên**.

## Global Constraints

- **Python:** container 3.12, dev 3.13. **Không dùng cú pháp chỉ có ở 3.13+.**
- **Pin version:** `fastapi>=0.115`, `uvicorn[standard]>=0.30`, `httpx>=0.27`, `python-multipart>=0.0.9` (bắt buộc cho upload), `pandas>=2.2,<3`, `mlflow>=2.14,<3`.
- **Dashboard chỉ được gọi `services/api/`.** Không bao giờ gọi thẳng Airflow (8080), MLflow (5000) hay MinIO (9000). Plan 5b sẽ có bước grep kiểm điều này.
- **Mọi key object storage** phải sinh từ hàm `*_key()` / `*_prefix()` trong `common/ml_common/storage.py`. Không nối chuỗi ở nơi khác.
- **Không sửa `services/serving/`, `stages/`, `common/ml_common/`.** API chỉ **đọc** thứ chúng ghi ra. Ngoại lệ duy nhất là `dags/ml_pipeline_dag.py` ở Task 6, và lý do nằm ở spec mục 2.3.
- **Thống kê cột dùng lại `ml_common.validation`**, không tự tính. Hai bản đếm missing rate là hai bản sẽ lệch nhau.
- **Docstring:** mọi function/method/class trong code production phải có docstring Google style với `Args:`, `Returns:`, thêm `Raises:` khi có ném exception, và `Example:` dạng minh hoạ dùng `# ->` (**không dùng `>>>`**). Thư mục `tests/` được miễn rule `D` nhưng **không** miễn `F` — không được có import thừa.
- **Ruff:** cấu hình ở `ruff.toml` **gốc repo**. `line-length = 100`, rules `["E","F","I","UP","B","D"]`, `convention = "google"`. Chạy `.venv\Scripts\python.exe -m ruff check . --fix`.
- **Ngôn ngữ trong code:** tiếng Anh tự nhiên. File markdown tiếng Việt có dấu.
- **Giá trị thiếu:** `None` / `np.nan`. Không dùng chuỗi rỗng hay `-1`.
- **Commit:** mỗi task **đúng một** commit, Conventional Commits, mô tả **tiếng Anh**, kết thúc bằng `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Kiểm lại bằng `git log -1`.
- **Build lại ba tầng khi `common/` đổi**, đúng thứ tự: `scripts\build_base_image.ps1` → `scripts\build_stage_images.ps1` → build lại `ml-serving`, `ml-agent`, `ml-api`. Chỉ build `ml-base` là **chưa đủ**.
- **Lệnh Python local:** gọi thẳng `.venv\Scripts\python.exe`, không dùng `Activate.ps1`.

## File Structure

| File | Trách nhiệm |
| --- | --- |
| `docker-compose.yml` | **Sửa:** `AIRFLOW__API__AUTH_BACKENDS` vào anchor `&airflow-env`; thêm service `api` |
| `services/api/app.py` | **Mới.** `create_app()` + wiring. Không chứa logic nghiệp vụ |
| `services/api/deps.py` | **Mới.** `require_auth()` rỗng — chỗ duy nhất sẽ thêm API key sau này |
| `services/api/clients/airflow.py` | **Mới.** Gọi Airflow REST, chuẩn hoá phản hồi |
| `services/api/clients/registry.py` | **Mới.** Gọi MLflow, trả version + metric động |
| `services/api/clients/reports.py` | **Mới.** Đọc drift summary từ object storage |
| `services/api/routes/pipeline.py` | **Mới.** 4 endpoint pipeline |
| `services/api/routes/data.py` | **Mới.** upload + preview |
| `services/api/routes/models.py` | **Mới.** list + promote |
| `services/api/routes/drift.py` | **Mới.** latest + history |
| `services/api/routes/health.py` | **Mới.** trạng thái 5 phụ thuộc |
| `services/api/Dockerfile` | **Mới.** `FROM ml-base:latest` + FastAPI |
| `dags/ml_pipeline_dag.py` | **Sửa:** `sample_rows` thành param |
| `scripts/verify_api.ps1` | **Mới.** Một lệnh xác nhận Plan 5a |
| `docs/superpowers/specs/2026-09-20-plan5b-ui-brief.md` | **Mới.** Đầu vào cho Claude design, trích JSON thật |

**Vì sao `clients/` tách khỏi `routes/`:** client test được bằng HTTP giả mà không cần FastAPI; route test được bằng client giả mà không cần Airflow. Gộp lại thì mọi test đều cần cả hai.

---

## Task 1: Bật basic auth cho Airflow REST — chặn đường

Spec mục 2.1: đo ngày 2026-09-20, `curl -u admin:admin .../api/v1/dags` trả **401**, và `airflow config get-value api auth_backends` trả `airflow.api.auth.backend.session`. Không service nào gọi được REST cho tới khi sửa. Làm trước mọi dòng route.

**Files:**
- Modify: `docker-compose.yml`

**Interfaces:**
- Produces: Airflow REST nhận basic auth; `curl -u admin:admin http://localhost:8080/api/v1/dags` trả 200

- [ ] **Step 1: Xác nhận hiện trạng 401**

```powershell
curl.exe -s -o NUL -w "%{http_code}`n" -u admin:admin http://localhost:8080/api/v1/dags
```

Expected: `401`. Nếu đã là 200 thì ai đó sửa rồi — ghi vào report và bỏ qua Step 2–3.

- [ ] **Step 2: Thêm auth backend vào anchor**

`docker-compose.yml` dùng YAML anchor `environment: &airflow-env` (khoảng dòng 89) rồi tái dùng bằng `<<: *airflow-env`. Sửa **một chỗ** là cả webserver lẫn scheduler đều nhận.

Thêm vào khối `&airflow-env`:

```yaml
      # Airflow 2.10 chi bat session backend, nen moi loi goi basic auth tu
      # services/api/ deu bi 401 - va thong diep 401 khong he goi y rang
      # nguyen nhan la thieu backend chu khong phai sai mat khau.
      # Giu session phia sau de dang nhap tren trinh duyet khong doi.
      AIRFLOW__API__AUTH_BACKENDS: "airflow.api.auth.backend.basic_auth,airflow.api.auth.backend.session"
```

- [ ] **Step 3: Khởi động lại và chứng minh 200**

```powershell
docker compose up -d --force-recreate airflow-webserver airflow-scheduler
```

Đợi webserver healthy (có thể mất ~60 giây), rồi:

```powershell
docker compose exec -T airflow-webserver airflow config get-value api auth_backends
curl.exe -s -o NUL -w "%{http_code}`n" -u admin:admin http://localhost:8080/api/v1/dags
```

Expected: config in ra cả `basic_auth` lẫn `session`; curl trả **200**.

**Dán cả hai output thật vào report.** Nếu vẫn 401, dừng và báo — đừng viết route trên một nền chưa chứng minh được.

- [ ] **Step 4: Xác nhận UI trình duyệt vẫn đăng nhập được**

Mở http://localhost:8080, đăng nhập `admin`/`admin`. Expected: vào được như cũ. Đây là lý do giữ `session` trong danh sách.

- [ ] **Step 5: Commit**

```bash
git add docker-compose.yml
git commit -m "$(cat <<'EOF'
build: let Airflow's REST API accept basic auth

Airflow 2.10 ships only the session backend, so every call from a service
using basic auth gets 401 - and the 401 says nothing about a missing
backend, which makes it read like a wrong password. services/api/ cannot
reach Airflow at all until this changes.

session stays in the list behind basic_auth so signing in through the
browser keeps working.

Verified: auth_backends now reports both, and GET /api/v1/dags returns 200
where it returned 401 before.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Bộ khung `services/api/` và endpoint `/health`

**Files:**
- Create: `services/api/__init__.py`, `services/api/app.py`, `services/api/deps.py`, `services/api/routes/__init__.py`, `services/api/routes/health.py`, `services/api/Dockerfile`, `services/api/tests/__init__.py`, `services/api/tests/test_health.py`
- Modify: `docker-compose.yml`

**Interfaces:**
- Produces:
  - `app.create_app(airflow=None, registry=None, reports=None, probes=None) -> FastAPI`
  - `deps.require_auth() -> None`
  - `GET /api/health` → `{"status": str, "services": dict[str, str]}`

- [ ] **Step 1: Viết test fail trước**

Tạo `services/api/tests/__init__.py` (rỗng) và `services/api/tests/test_health.py`:

```python
from fastapi.testclient import TestClient

from services.api.app import create_app


def _client(probes: dict) -> TestClient:
    return TestClient(create_app(probes=probes))


def test_health_is_ok_when_every_dependency_answers():
    client = _client({"airflow": lambda: True, "mlflow": lambda: True})
    body = client.get("/api/health").json()
    assert body["status"] == "ok"
    assert body["services"] == {"airflow": "ok", "mlflow": "ok"}


def test_health_is_degraded_when_one_dependency_is_down():
    client = _client({"airflow": lambda: True, "mlflow": lambda: False})
    body = client.get("/api/health").json()
    assert body["status"] == "degraded"
    assert body["services"]["mlflow"] == "down"


def test_a_probe_that_raises_counts_as_down_not_a_crash():
    def explode():
        raise RuntimeError("connection refused")

    client = _client({"airflow": explode})
    response = client.get("/api/health")
    assert response.status_code == 200
    assert response.json()["services"]["airflow"] == "down"


def test_health_always_returns_200_even_when_everything_is_down():
    client = _client({"airflow": lambda: False, "mlflow": lambda: False})
    response = client.get("/api/health")
    assert response.status_code == 200
    assert response.json()["status"] == "degraded"
```

- [ ] **Step 2: Chạy cho fail**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_health.py -v
```

Expected: FAIL với `ModuleNotFoundError: No module named 'services.api'`

- [ ] **Step 3: Implement**

`services/api/__init__.py`:

```python
"""API layer between the dashboard and the rest of the pipeline."""
```

`services/api/deps.py`:

```python
"""Shared FastAPI dependencies.

`require_auth` does nothing today. Section 3.2 of the design doc says auth
is not needed while this runs locally for one person, but that it becomes
mandatory before anything is exposed. Declaring the dependency now means
that change touches ONE function instead of eleven routes, with no chance
of missing one.
"""

from __future__ import annotations


def require_auth() -> None:
    """Authorises the caller.

    Args:
        None.

    Returns:
        Nothing. Today every caller is allowed: the stack runs on localhost
        for a single user. When an API key is added, this is the only place
        that changes.

    Example:
        # Every route declares it, so none of them has to be edited later:
        @router.get("/models", dependencies=[Depends(require_auth)])
        def list_models():
            ...
    """
    return None
```

`services/api/routes/__init__.py` (rỗng).

`services/api/routes/health.py`:

```python
"""Reports whether each thing the dashboard depends on is reachable."""

from __future__ import annotations

from fastapi import APIRouter, Depends

from ..deps import require_auth

router = APIRouter()


def build_health(probes: dict) -> dict:
    """Runs every dependency probe and summarises the result.

    Args:
        probes: name to a zero-argument callable returning True when that
            dependency answers. A probe that raises counts as down - the
            dashboard needs an answer, not a traceback.

    Returns:
        `status`, "ok" when every probe passed and "degraded" otherwise, and
        `services`, each probe's name mapped to "ok" or "down".

    Example:
        build_health({"airflow": lambda: True, "mlflow": lambda: False})
        # -> {"status": "degraded",
        #     "services": {"airflow": "ok", "mlflow": "down"}}
    """
    services: dict[str, str] = {}
    for name, probe in probes.items():
        try:
            services[name] = "ok" if probe() else "down"
        except Exception:  # noqa: BLE001 - any failure means "not reachable"
            services[name] = "down"
    healthy = all(state == "ok" for state in services.values())
    return {"status": "ok" if healthy else "degraded", "services": services}
```

`services/api/app.py`:

```python
"""HTTP surface the dashboard talks to, and nothing else talks to.

Every collaborator is injected, the same way `services/serving/app.py`
injects its registry and its flush function. That is what lets the routes
be tested without Airflow, MLflow or MinIO running - serving's suite proves
the pattern works, at 20 tests in four seconds.

The dashboard reaches Airflow, MLflow and object storage ONLY through here.
Section 8.2 of the design doc gives the reason: credentials would otherwise
live in the browser, CORS would have to open for several services, and
every backend swap would become a frontend change.
"""

from __future__ import annotations

from fastapi import FastAPI

from .routes import health


def create_app(
    airflow=None,
    registry=None,
    reports=None,
    probes: dict | None = None,
) -> FastAPI:
    """Builds the API application.

    Args:
        airflow: client for Airflow's REST API. None builds the real one.
        registry: client for the MLflow Model Registry. None builds the real one.
        reports: reader for drift summaries in object storage. None builds
            the real one.
        probes: health probes, name to a zero-argument callable. None builds
            one probe per real dependency.

    Returns:
        A FastAPI app serving everything under `/api`.

    Example:
        # Production - module level, everything real:
        app = create_app()

        # A test - no infrastructure at all:
        app = create_app(probes={"airflow": lambda: True})
    """
    app = FastAPI(title="mlops control room api")
    app.state.airflow = airflow
    app.state.registry = registry
    app.state.reports = reports
    app.state.probes = probes if probes is not None else {}

    app.include_router(health.router, prefix="/api")
    return app


app = create_app()
```

Sửa `health.py` để lấy probes từ app state — thêm route:

```python
@router.get("/health", dependencies=[Depends(require_auth)])
def health(request) -> dict:
    """Reports the state of every dependency the dashboard needs.

    Args:
        request: the FastAPI request, used to read the injected probes.

    Returns:
        `status` ("ok" or "degraded") and `services`. Always HTTP 200 -
        "degraded" is a state to read, not a request that failed, the same
        contract serving's /health has used since plan 3.

    Example:
        # GET /api/health
        # {"status": "degraded",
        #  "services": {"airflow": "ok", "mlflow": "ok", "minio": "ok",
        #               "serving": "down", "postgres": "ok"}}
    """
    return build_health(request.app.state.probes)
```

Thêm import `Request` từ fastapi và đổi chữ ký thành `def health(request: Request) -> dict:`.

- [ ] **Step 4: Chạy cho pass**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_health.py -v
.venv\Scripts\python.exe -m ruff check . --fix
```

Expected: 4 passed, ruff sạch.

- [ ] **Step 5: Dockerfile**

Tạo `services/api/Dockerfile`, theo đúng khuôn của `services/serving/Dockerfile`:

```dockerfile
# API layer. Build from the repo root:
#   docker build -f services/api/Dockerfile -t ml-api:latest .
#
# FROM ml-base because this service reads what the pipeline wrote, using the
# same ml_common.storage key functions. Rebuilding those paths here would be
# a second copy of the one thing only storage.py is allowed to know.
FROM ml-base:latest

RUN pip install --no-cache-dir \
    "fastapi>=0.115" "uvicorn[standard]>=0.30" \
    "httpx>=0.27" "python-multipart>=0.0.9"

COPY services/ /app/services/

ENV PYTHONPATH=/app
EXPOSE 8001

CMD ["uvicorn", "services.api.app:app", "--host", "0.0.0.0", "--port", "8001"]
```

Cổng 8001 vì serving đã chiếm 8000.

- [ ] **Step 6: Service trong compose**

Thêm vào `docker-compose.yml`, cạnh `serving`:

```yaml
  api:
    build:
      context: .
      dockerfile: services/api/Dockerfile
    image: ml-api:latest
    ports:
      - "8001:8001"
    depends_on:
      - minio
      - mlflow
      - serving
    environment:
      MINIO_ENDPOINT_INTERNAL: ${MINIO_ENDPOINT_INTERNAL:-http://minio:9000}
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY}
      ML_BUCKET: ${ML_BUCKET:-ml-pipeline}
      MLFLOW_TRACKING_URI: ${MLFLOW_TRACKING_URI:-http://mlflow:5000}
      MLFLOW_S3_ENDPOINT_URL: ${MINIO_ENDPOINT_INTERNAL:-http://minio:9000}
      AWS_ACCESS_KEY_ID: ${MINIO_ACCESS_KEY}
      AWS_SECRET_ACCESS_KEY: ${MINIO_SECRET_KEY}
      AIRFLOW_API_URL: http://airflow-webserver:8080/api/v1
      AIRFLOW_USERNAME: ${AIRFLOW_USERNAME:-admin}
      AIRFLOW_PASSWORD: ${AIRFLOW_PASSWORD:-admin}
      SERVING_URL: http://serving:8000
```

- [ ] **Step 7: Build và chạy thử**

```powershell
docker build -f services/api/Dockerfile -t ml-api:latest .
docker compose up -d api
curl.exe -s http://localhost:8001/api/health
```

Expected: build xong, JSON trả về có `status` và `services` (lúc này `services` rỗng vì chưa nối probe thật — đúng, Task 11 nối).

- [ ] **Step 8: Commit**

```bash
git add services/api/ docker-compose.yml
git commit -m "$(cat <<'EOF'
feat: API skeleton with an injectable app and a health endpoint

create_app takes every collaborator as an argument, the way serving's does,
so routes can be tested with nothing running. That pattern is why serving's
suite finishes in four seconds without MLflow or MinIO.

/api/health always answers 200: degraded is a state the dashboard reads,
not a failed request. A probe that raises counts as down rather than
taking the endpoint with it - the dashboard needs an answer, not a
traceback.

require_auth exists and does nothing. Section 3.2 says auth waits until
this is exposed; declaring the dependency now means that change touches one
function rather than eleven routes.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `clients/airflow.py` — nói chuyện với Airflow REST

**Files:**
- Create: `services/api/clients/__init__.py`, `services/api/clients/airflow.py`, `services/api/tests/test_airflow_client.py`

**Interfaces:**
- Produces:
  - `AirflowClient(base_url, username, password, http=None)`
  - `.trigger_run(dag_id: str, conf: dict) -> dict` → `{"run_id", "dag_id", "state"}`
  - `.list_runs(dag_id: str, limit: int) -> list[dict]`
  - `.get_run(dag_id: str, run_id: str) -> dict` → `{"run_id", "state", "tasks": [...]}`
  - `.get_logs(dag_id, run_id, task_id, try_number) -> str`

- [ ] **Step 1: Viết test fail trước**

Tạo `services/api/tests/test_airflow_client.py`:

```python
import pytest

from services.api.clients.airflow import AirflowClient


class FakeResponse:
    def __init__(self, payload, status_code=200):
        self._payload = payload
        self.status_code = status_code
        self.text = str(payload)

    def json(self):
        return self._payload

    def raise_for_status(self):
        if self.status_code >= 400:
            raise RuntimeError(f"HTTP {self.status_code}")


class FakeHttp:
    """Records requests instead of making them."""

    def __init__(self, responses):
        self.responses = list(responses)
        self.calls = []

    def request(self, method, url, **kwargs):
        self.calls.append((method, url, kwargs))
        return self.responses.pop(0)


def _client(responses):
    return AirflowClient("http://airflow:8080/api/v1", "admin", "admin", http=FakeHttp(responses))


def test_trigger_run_posts_conf_and_returns_the_run_id():
    http = FakeHttp([FakeResponse({"dag_run_id": "manual__2026", "state": "queued"})])
    client = AirflowClient("http://airflow:8080/api/v1", "admin", "admin", http=http)

    result = client.trigger_run("ml_pipeline", {"task_type": "regression"})

    method, url, kwargs = http.calls[0]
    assert method == "POST"
    assert url == "http://airflow:8080/api/v1/dags/ml_pipeline/dagRuns"
    assert kwargs["json"]["conf"] == {"task_type": "regression"}
    assert result == {"run_id": "manual__2026", "dag_id": "ml_pipeline", "state": "queued"}


def test_every_request_carries_basic_auth():
    http = FakeHttp([FakeResponse({"dag_run_id": "r", "state": "queued"})])
    client = AirflowClient("http://airflow:8080/api/v1", "admin", "secret", http=http)

    client.trigger_run("ml_pipeline", {})

    assert http.calls[0][2]["auth"] == ("admin", "secret")


def test_list_runs_normalises_the_fields_the_dashboard_needs():
    http = FakeHttp(
        [
            FakeResponse(
                {
                    "dag_runs": [
                        {
                            "dag_run_id": "r1",
                            "state": "success",
                            "start_date": "2026-09-20T10:00:00+00:00",
                            "end_date": "2026-09-20T10:09:00+00:00",
                            "conf": {"task_type": "regression"},
                        }
                    ]
                }
            )
        ]
    )
    client = AirflowClient("http://airflow:8080/api/v1", "admin", "admin", http=http)

    runs = client.list_runs("ml_pipeline", limit=5)

    assert runs == [
        {
            "run_id": "r1",
            "state": "success",
            "task_type": "regression",
            "started_at": "2026-09-20T10:00:00+00:00",
            "ended_at": "2026-09-20T10:09:00+00:00",
        }
    ]


def test_list_runs_survives_a_run_with_no_conf():
    # A run triggered from the Airflow UI with no config has conf {}.
    http = FakeHttp(
        [
            FakeResponse(
                {
                    "dag_runs": [
                        {
                            "dag_run_id": "r1",
                            "state": "running",
                            "start_date": "2026-09-20T10:00:00+00:00",
                            "end_date": None,
                            "conf": {},
                        }
                    ]
                }
            )
        ]
    )
    client = AirflowClient("http://airflow:8080/api/v1", "admin", "admin", http=http)

    runs = client.list_runs("ml_pipeline", limit=5)

    assert runs[0]["task_type"] is None
    assert runs[0]["ended_at"] is None


def test_list_runs_asks_for_newest_first():
    http = FakeHttp([FakeResponse({"dag_runs": []})])
    client = AirflowClient("http://airflow:8080/api/v1", "admin", "admin", http=http)

    client.list_runs("ml_pipeline", limit=7)

    params = http.calls[0][2]["params"]
    assert params["limit"] == 7
    assert params["order_by"] == "-execution_date"


def test_get_run_returns_task_states():
    http = FakeHttp(
        [
            FakeResponse({"dag_run_id": "r1", "state": "running"}),
            FakeResponse(
                {
                    "task_instances": [
                        {
                            "task_id": "extract",
                            "state": "success",
                            "try_number": 1,
                            "duration": 12.5,
                        },
                        {
                            "task_id": "validate",
                            "state": "running",
                            "try_number": 1,
                            "duration": None,
                        },
                    ]
                }
            ),
        ]
    )
    client = AirflowClient("http://airflow:8080/api/v1", "admin", "admin", http=http)

    run = client.get_run("ml_pipeline", "r1")

    assert run["run_id"] == "r1"
    assert run["state"] == "running"
    assert run["tasks"] == [
        {"task_id": "extract", "state": "success", "try_number": 1, "duration": 12.5},
        {"task_id": "validate", "state": "running", "try_number": 1, "duration": None},
    ]


def test_get_logs_returns_the_raw_text():
    http = FakeHttp([FakeResponse({"content": "line one\nline two"})])
    client = AirflowClient("http://airflow:8080/api/v1", "admin", "admin", http=http)

    text = client.get_logs("ml_pipeline", "r1", "extract", 1)

    assert text == "line one\nline two"
    assert "taskInstances/extract/logs/1" in http.calls[0][1]


def test_an_http_error_propagates_rather_than_being_swallowed():
    http = FakeHttp([FakeResponse({"detail": "not found"}, status_code=404)])
    client = AirflowClient("http://airflow:8080/api/v1", "admin", "admin", http=http)

    with pytest.raises(RuntimeError):
        client.get_run("ml_pipeline", "missing")
```

- [ ] **Step 2: Chạy cho fail**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_airflow_client.py -v
```

Expected: FAIL với `ModuleNotFoundError: No module named 'services.api.clients'`

- [ ] **Step 3: Implement**

`services/api/clients/__init__.py`:

```python
"""Clients that know what the systems behind the API actually return."""
```

`services/api/clients/airflow.py`:

```python
"""Talks to Airflow's REST API and hands back shapes the routes can use.

Airflow's payloads carry far more than a dashboard needs and use names from
its own domain. Normalising here means the routes - and the frontend behind
them - never learn what a `dag_run_id` or an `execution_date` is, so
swapping Airflow for MWAA later changes this file and nothing else.

Basic auth is used on every call, which requires
AIRFLOW__API__AUTH_BACKENDS to include basic_auth. Airflow 2.10 ships only
the session backend and answers 401 without saying why.
"""

from __future__ import annotations

import httpx

DEFAULT_TIMEOUT = 30.0


class AirflowClient:
    """A thin, normalising wrapper over the Airflow REST API.

    Example:
        client = AirflowClient("http://airflow-webserver:8080/api/v1", "admin", "admin")
        client.trigger_run("ml_pipeline", {"task_type": "regression"})
        # -> {"run_id": "manual__2026-09-20T...", "dag_id": "ml_pipeline",
        #     "state": "queued"}
    """

    def __init__(self, base_url: str, username: str, password: str, http=None):
        """Records where Airflow is and how to authenticate to it.

        Args:
            base_url: the API root, e.g. "http://airflow-webserver:8080/api/v1".
            username: Airflow user with permission to trigger DAGs.
            password: that user's password.
            http: something with `.request(method, url, **kwargs)` returning a
                response. None builds a real httpx client. Injected so the
                tests need no Airflow.
        """
        self._base = base_url.rstrip("/")
        self._auth = (username, password)
        self._http = http if http is not None else httpx.Client(timeout=DEFAULT_TIMEOUT)

    def _call(self, method: str, path: str, **kwargs) -> dict:
        """Makes one authenticated request and returns the decoded body.

        Args:
            method: "GET" or "POST".
            path: path below the API root, starting with "/".
            **kwargs: passed through to the HTTP client.

        Returns:
            The decoded JSON body.

        Raises:
            Exception: whatever the HTTP client raises for a non-2xx status.
                Errors are not swallowed here - a route that cannot reach
                Airflow must say so rather than return an empty list that
                looks like "nothing has run yet".

        Example:
            self._call("GET", "/dags/ml_pipeline/dagRuns", params={"limit": 5})
        """
        response = self._http.request(method, f"{self._base}{path}", auth=self._auth, **kwargs)
        response.raise_for_status()
        return response.json()

    def trigger_run(self, dag_id: str, conf: dict) -> dict:
        """Starts a DAG run and returns its identifier.

        Args:
            dag_id: the DAG to start, e.g. "ml_pipeline".
            conf: the run configuration, which becomes `params` inside the DAG.

        Returns:
            `run_id`, `dag_id` and `state`. The run has only been QUEUED -
            it has not finished, and on this pipeline it will take minutes.

        Example:
            trigger_run("ml_pipeline", {"task_type": "regression", "sample_rows": 1000})
            # -> {"run_id": "manual__2026-09-20T10:00:00+00:00",
            #     "dag_id": "ml_pipeline", "state": "queued"}
        """
        body = self._call("POST", f"/dags/{dag_id}/dagRuns", json={"conf": conf})
        return {"run_id": body["dag_run_id"], "dag_id": dag_id, "state": body["state"]}

    def list_runs(self, dag_id: str, limit: int) -> list[dict]:
        """Lists recent runs, newest first.

        Args:
            dag_id: the DAG to list.
            limit: how many runs to return.

        Returns:
            One dict per run with `run_id`, `state`, `task_type`, `started_at`
            and `ended_at`. `task_type` comes from the run's conf and is None
            for a run triggered from the Airflow UI without one. `ended_at` is
            None while the run is still going.

        Example:
            list_runs("ml_pipeline", limit=5)
            # -> [{"run_id": "manual__...", "state": "success",
            #      "task_type": "regression", "started_at": "...",
            #      "ended_at": "..."}]
        """
        body = self._call(
            "GET",
            f"/dags/{dag_id}/dagRuns",
            params={"limit": limit, "order_by": "-execution_date"},
        )
        return [
            {
                "run_id": run["dag_run_id"],
                "state": run["state"],
                "task_type": (run.get("conf") or {}).get("task_type"),
                "started_at": run.get("start_date"),
                "ended_at": run.get("end_date"),
            }
            for run in body.get("dag_runs", [])
        ]

    def get_run(self, dag_id: str, run_id: str) -> dict:
        """Reads one run together with the state of each of its tasks.

        Args:
            dag_id: the DAG the run belongs to.
            run_id: the run identifier.

        Returns:
            `run_id`, `state`, and `tasks` - one entry per task with
            `task_id`, `state`, `try_number` and `duration` (None while the
            task is still running).

        Raises:
            Exception: when the run does not exist. Returning an empty task
                list instead would look like a run that did nothing.

        Example:
            get_run("ml_pipeline", "manual__2026-09-20T10:00:00+00:00")
            # -> {"run_id": "...", "state": "running",
            #     "tasks": [{"task_id": "extract", "state": "success", ...}]}
        """
        run = self._call("GET", f"/dags/{dag_id}/dagRuns/{run_id}")
        instances = self._call("GET", f"/dags/{dag_id}/dagRuns/{run_id}/taskInstances")
        return {
            "run_id": run["dag_run_id"],
            "state": run["state"],
            "tasks": [
                {
                    "task_id": task["task_id"],
                    "state": task["state"],
                    "try_number": task.get("try_number"),
                    "duration": task.get("duration"),
                }
                for task in instances.get("task_instances", [])
            ],
        }

    def get_logs(self, dag_id: str, run_id: str, task_id: str, try_number: int) -> str:
        """Fetches the log of one task attempt.

        Args:
            dag_id: the DAG the run belongs to.
            run_id: the run identifier.
            task_id: which task's log to read.
            try_number: which attempt; Airflow numbers them from 1.

        Returns:
            The log as one block of text. Splitting and filtering happen in
            the route, so this stays a transport concern.

        Example:
            get_logs("ml_pipeline", "manual__...", "extract", 1)
            # -> "[2026-09-20 10:00:01] INFO - raw=raw/v1/data.parquet ..."
        """
        body = self._call(
            "GET",
            f"/dags/{dag_id}/dagRuns/{run_id}/taskInstances/{task_id}/logs/{try_number}",
        )
        return body.get("content", "") if isinstance(body, dict) else str(body)
```

- [ ] **Step 4: Chạy cho pass**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_airflow_client.py -v
.venv\Scripts\python.exe -m ruff check . --fix
```

Expected: 8 passed, ruff sạch.

- [ ] **Step 5: Commit**

```bash
git add services/api/clients/ services/api/tests/test_airflow_client.py
git commit -m "$(cat <<'EOF'
feat: Airflow REST client that normalises what the dashboard sees

Airflow's payloads carry its own vocabulary - dag_run_id, execution_date,
task_instances - and far more fields than a dashboard needs. Translating
here means the routes and the frontend never learn any of it, so swapping
Airflow for MWAA later changes this file alone.

Errors propagate rather than being swallowed. A client that turns an
unreachable Airflow into an empty list produces a dashboard that says
"nothing has run yet", which is the wrong answer to a different question.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `routes/pipeline.py` — trigger và trạng thái run

**Files:**
- Create: `services/api/routes/pipeline.py`, `services/api/tests/test_pipeline.py`
- Modify: `services/api/app.py`

**Interfaces:**
- Consumes: `AirflowClient.trigger_run/list_runs/get_run`
- Produces: `POST /api/pipeline/run`, `GET /api/pipeline/runs`, `GET /api/pipeline/runs/{run_id}`

- [ ] **Step 1: Viết test fail trước**

Tạo `services/api/tests/test_pipeline.py`:

```python
from fastapi.testclient import TestClient

from services.api.app import create_app


class FakeAirflow:
    """Stands in for AirflowClient, recording what the routes asked for."""

    def __init__(self, runs=None, run=None):
        self.triggered = []
        self.runs = runs if runs is not None else []
        self.run = run

    def trigger_run(self, dag_id, conf):
        self.triggered.append((dag_id, conf))
        return {"run_id": "manual__2026", "dag_id": dag_id, "state": "queued"}

    def list_runs(self, dag_id, limit):
        return self.runs[:limit]

    def get_run(self, dag_id, run_id):
        return self.run


def _client(airflow):
    return TestClient(create_app(airflow=airflow))


def test_run_triggers_the_dag_and_returns_the_run_id():
    airflow = FakeAirflow()
    response = _client(airflow).post(
        "/api/pipeline/run",
        json={"task_type": "regression", "force_reprocess": False, "sample_rows": 1000},
    )

    assert response.status_code == 200
    assert response.json()["run_id"] == "manual__2026"
    dag_id, conf = airflow.triggered[0]
    assert dag_id == "ml_pipeline"
    assert conf["task_type"] == "regression"
    assert conf["sample_rows"] == 1000


def test_run_defaults_sample_rows_to_none_meaning_every_row():
    airflow = FakeAirflow()
    _client(airflow).post("/api/pipeline/run", json={"task_type": "regression"})

    _, conf = airflow.triggered[0]
    assert conf["sample_rows"] is None


def test_run_rejects_an_unknown_task_type():
    response = _client(FakeAirflow()).post("/api/pipeline/run", json={"task_type": "nonsense"})
    assert response.status_code == 422


def test_run_rejects_a_zero_or_negative_sample_rows():
    client = _client(FakeAirflow())
    for bad in (0, -1):
        response = client.post(
            "/api/pipeline/run", json={"task_type": "regression", "sample_rows": bad}
        )
        assert response.status_code == 422, bad


def test_runs_lists_recent_runs():
    airflow = FakeAirflow(
        runs=[{"run_id": "r1", "state": "success", "task_type": "regression",
               "started_at": "t", "ended_at": "t"}]
    )
    body = _client(airflow).get("/api/pipeline/runs").json()
    assert body["runs"][0]["run_id"] == "r1"


def test_runs_honours_the_limit_query():
    airflow = FakeAirflow(runs=[{"run_id": f"r{i}"} for i in range(10)])
    body = _client(airflow).get("/api/pipeline/runs?limit=3").json()
    assert len(body["runs"]) == 3


def test_run_detail_returns_task_states():
    airflow = FakeAirflow(
        run={"run_id": "r1", "state": "running",
             "tasks": [{"task_id": "extract", "state": "success",
                        "try_number": 1, "duration": 12.5}]}
    )
    body = _client(airflow).get("/api/pipeline/runs/r1").json()
    assert body["tasks"][0]["task_id"] == "extract"
```

- [ ] **Step 2: Chạy cho fail**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_pipeline.py -v
```

Expected: FAIL — 404 trên mọi route, vì chưa đăng ký router.

- [ ] **Step 3: Implement**

`services/api/routes/pipeline.py`:

```python
"""Starting pipeline runs and reporting what they are doing.

A run takes minutes, so every endpoint here is a snapshot: trigger returns
as soon as Airflow has queued the run, and the dashboard polls for the rest.
"""

from __future__ import annotations

from typing import Literal

from fastapi import APIRouter, Depends, Request
from pydantic import BaseModel, Field

from ..deps import require_auth

router = APIRouter()

DAG_ID = "ml_pipeline"
DEFAULT_RUN_LIMIT = 20


class RunRequest(BaseModel):
    """What the dashboard sends to start a pipeline run.

    Example:
        # POST /api/pipeline/run
        # {"task_type": "regression", "force_reprocess": false,
        #  "sample_rows": 1000, "dataset_version": "v1"}
    """

    task_type: Literal["regression", "classification"]
    force_reprocess: bool = False
    sample_rows: int | None = Field(default=None, gt=0)
    dataset_version: str = "v1"


@router.post("/pipeline/run", dependencies=[Depends(require_auth)])
def trigger_run(request: Request, body: RunRequest) -> dict:
    """Queues a pipeline run with the settings the dashboard chose.

    Args:
        request: the FastAPI request, used to reach the injected client.
        body: the run settings. `sample_rows` None means every row; a value
            of 0 or less is rejected rather than silently treated as "all",
            which would start a two-million-row run on a machine that asked
            for a small one.

    Returns:
        `run_id`, `dag_id` and `state`. The run is only QUEUED - it has not
        finished, and it will take minutes.

    Example:
        # -> {"run_id": "manual__2026-09-20T10:00:00+00:00",
        #     "dag_id": "ml_pipeline", "state": "queued"}
    """
    conf = {
        "task_type": body.task_type,
        "force_reprocess": body.force_reprocess,
        "sample_rows": body.sample_rows,
        "dataset_version": body.dataset_version,
    }
    return request.app.state.airflow.trigger_run(DAG_ID, conf)


@router.get("/pipeline/runs", dependencies=[Depends(require_auth)])
def list_runs(request: Request, limit: int = DEFAULT_RUN_LIMIT) -> dict:
    """Lists recent pipeline runs, newest first.

    Args:
        request: the FastAPI request.
        limit: how many runs to return.

    Returns:
        `runs`, a list of run summaries. Wrapped in an object rather than
        returned as a bare array so fields can be added later without
        breaking the frontend.

    Example:
        # GET /api/pipeline/runs?limit=5
        # {"runs": [{"run_id": "...", "state": "success",
        #            "task_type": "regression", ...}]}
    """
    return {"runs": request.app.state.airflow.list_runs(DAG_ID, limit)}


@router.get("/pipeline/runs/{run_id}", dependencies=[Depends(require_auth)])
def get_run(request: Request, run_id: str) -> dict:
    """Reports one run and the state of each of its tasks.

    Args:
        request: the FastAPI request.
        run_id: the run to describe.

    Returns:
        `run_id`, `state` and `tasks`. This is what draws the stage strip on
        the overview screen.

    Example:
        # GET /api/pipeline/runs/manual__2026-...
        # {"run_id": "...", "state": "running",
        #  "tasks": [{"task_id": "extract", "state": "success",
        #             "try_number": 1, "duration": 12.5}]}
    """
    return request.app.state.airflow.get_run(DAG_ID, run_id)
```

Sửa `services/api/app.py`: thêm `from .routes import health, pipeline` và `app.include_router(pipeline.router, prefix="/api")`.

- [ ] **Step 4: Chạy cho pass**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/ -v
.venv\Scripts\python.exe -m ruff check . --fix
```

Expected: 7 test mới pass, tổng file test_pipeline 7 passed.

- [ ] **Step 5: Commit**

```bash
git add services/api/routes/pipeline.py services/api/app.py services/api/tests/test_pipeline.py
git commit -m "$(cat <<'EOF'
feat: pipeline routes for triggering runs and reading their state

sample_rows is validated as greater than zero rather than being coerced.
Treating 0 as "use everything" would start a two-million-row run on a
machine that asked for a small one, and nothing downstream would question
it - the fingerprint would simply be the full-run fingerprint.

List responses are wrapped in an object instead of returning a bare array,
so a field can be added later without breaking the frontend.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: `GET /pipeline/runs/{run_id}/logs` — lọc ở backend

> **Đã làm rồi đã xoá (2026-09-22).** Chủ dự án bỏ tính năng xem log trên
> dashboard, nên endpoint này, `AirflowClient.get_logs` và bộ lọc log không còn
> trong code. Task giữ nguyên ở đây vì plan là biên bản việc đã làm — **đừng
> dựng lại theo nó**. Log của một task đọc thẳng trên Airflow UI.

**Files:**
- Modify: `services/api/routes/pipeline.py`, `services/api/tests/test_pipeline.py`

**Interfaces:**
- Consumes: `AirflowClient.get_logs`
- Produces: `GET /api/pipeline/runs/{run_id}/logs?stage=&level=&q=` → `{"lines": [...], "truncated": bool}`

- [ ] **Step 1: Viết test fail trước**

Thêm vào `services/api/tests/test_pipeline.py`:

```python
LOG_TEXT = "\n".join(
    [
        "[2026-09-20 10:00:01] INFO - reading raw data",
        "[2026-09-20 10:00:02] WARNING - 3 rows dropped",
        "[2026-09-20 10:00:03] ERROR - could not parse zipcode",
        "[2026-09-20 10:00:04] INFO - wrote 200000 rows",
    ]
)


class FakeAirflowLogs(FakeAirflow):
    def __init__(self, text=LOG_TEXT):
        super().__init__()
        self.text = text
        self.asked = []

    def get_logs(self, dag_id, run_id, task_id, try_number):
        self.asked.append((dag_id, run_id, task_id, try_number))
        return self.text


def test_logs_return_every_line_when_no_filter_is_given():
    body = _client(FakeAirflowLogs()).get("/api/pipeline/runs/r1/logs?stage=extract").json()
    assert len(body["lines"]) == 4
    assert body["truncated"] is False


def test_logs_filter_by_level():
    body = (
        _client(FakeAirflowLogs())
        .get("/api/pipeline/runs/r1/logs?stage=extract&level=ERROR")
        .json()
    )
    assert len(body["lines"]) == 1
    assert "could not parse zipcode" in body["lines"][0]


def test_logs_filter_by_keyword_case_insensitively():
    body = (
        _client(FakeAirflowLogs())
        .get("/api/pipeline/runs/r1/logs?stage=extract&q=ZIPCODE")
        .json()
    )
    assert len(body["lines"]) == 1


def test_logs_report_truncation_rather_than_hiding_it():
    long_text = "\n".join(f"[2026-09-20] INFO - line {i}" for i in range(3000))
    body = (
        _client(FakeAirflowLogs(text=long_text))
        .get("/api/pipeline/runs/r1/logs?stage=extract")
        .json()
    )
    assert body["truncated"] is True
    assert len(body["lines"]) == 2000


def test_logs_require_a_stage():
    response = _client(FakeAirflowLogs()).get("/api/pipeline/runs/r1/logs")
    assert response.status_code == 422
```

- [ ] **Step 2: Chạy cho fail**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_pipeline.py -k logs -v
```

Expected: FAIL — 404, route chưa tồn tại.

- [ ] **Step 3: Implement**

Thêm vào `services/api/routes/pipeline.py`:

```python
MAX_LOG_LINES = 2000


def filter_log_lines(text: str, level: str | None, keyword: str | None) -> tuple[list[str], bool]:
    """Splits a log into lines and keeps only the ones asked for.

    Args:
        text: the raw log text as Airflow returned it.
        level: keep only lines mentioning this level, case-insensitively.
            None keeps every level.
        keyword: keep only lines containing this text, case-insensitively.
            None keeps every line.

    Returns:
        A tuple of (lines, truncated). Filtering happens here rather than in
        the browser so a run with thousands of lines does not cross the
        network only to be thrown away - section 8.1 names doing this in JS
        as one of the things the old frontend got wrong. `truncated` is True
        when lines were dropped at MAX_LOG_LINES, so the UI can say so
        instead of silently showing less than there is.

    Example:
        filter_log_lines(text, level="ERROR", keyword=None)
        # -> (["[2026-09-20 10:00:03] ERROR - could not parse zipcode"], False)
    """
    lines = [line for line in text.splitlines() if line.strip()]
    if level:
        needle = level.lower()
        lines = [line for line in lines if needle in line.lower()]
    if keyword:
        needle = keyword.lower()
        lines = [line for line in lines if needle in line.lower()]
    truncated = len(lines) > MAX_LOG_LINES
    return lines[:MAX_LOG_LINES], truncated


@router.get("/pipeline/runs/{run_id}/logs", dependencies=[Depends(require_auth)])
def get_logs(
    request: Request,
    run_id: str,
    stage: str,
    level: str | None = None,
    q: str | None = None,
    try_number: int = 1,
) -> dict:
    """Reads one task's log, filtered on the server.

    Args:
        request: the FastAPI request.
        run_id: the run whose log is wanted.
        stage: which task's log - required, because a run has seven tasks and
            "the log" is not a thing that exists.
        level: keep only lines at this level.
        q: keep only lines containing this text.
        try_number: which attempt; Airflow numbers them from 1.

    Returns:
        `lines` and `truncated`.

    Example:
        # GET /api/pipeline/runs/manual__.../logs?stage=validate&level=ERROR
        # {"lines": ["[...] ERROR - target source missing in 62% of rows"],
        #  "truncated": false}
    """
    text = request.app.state.airflow.get_logs(DAG_ID, run_id, stage, try_number)
    lines, truncated = filter_log_lines(text, level, q)
    return {"lines": lines, "truncated": truncated}
```

- [ ] **Step 4: Chạy cho pass**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/ -q
.venv\Scripts\python.exe -m ruff check . --fix
```

- [ ] **Step 5: Commit**

```bash
git add services/api/routes/pipeline.py services/api/tests/test_pipeline.py
git commit -m "$(cat <<'EOF'
feat: task logs, filtered on the server

stage is required rather than optional. A run has seven tasks, so "the log"
is not a thing that exists, and defaulting to one of them would quietly
answer a question nobody asked.

Filtering happens here, not in the browser. Section 8.1 names client-side
filtering as one of the things the old frontend got wrong; a run with
thousands of lines should not cross the network to be discarded. When lines
are dropped at the cap, truncated says so - showing less than there is
without mentioning it is how people miss the error they were looking for.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `sample_rows` thành param của DAG

Spec mục 2.3. Đây là **thay đổi duy nhất** plan này được phép làm với code Plan 2.

**Files:**
- Modify: `dags/ml_pipeline_dag.py`

**Interfaces:**
- Produces: `ml_pipeline` nhận param `sample_rows`; stage `extract` nhận nó qua env `SAMPLE_ROWS`

- [ ] **Step 1: Đọc chỗ cần sửa**

```powershell
Select-String -Path dags\ml_pipeline_dag.py -Pattern "SAMPLE_ROWS|params=" -Context 2,4
```

Hiện tại dòng ~226 đọc `"SAMPLE_ROWS": os.environ.get("SAMPLE_ROWS", "")` — lấy từ môi trường scheduler, cố định cho mọi lần chạy.

- [ ] **Step 2: Thêm param**

Trong khối `params={...}` của `DAG(...)`, thêm:

```python
        # None = dung toan bo dong. Lay mac dinh tu moi truong de hanh vi
        # CLI hien tai khong doi, nhung dashboard ghi de duoc qua conf.
        "sample_rows": None,
```

- [ ] **Step 3: Truyền param xuống stage `extract`**

Đổi dòng `SAMPLE_ROWS` trong task `extract` thành template Jinja, giữ nguyên fallback môi trường:

```python
            # Uu tien param cua run; neu khong co thi dung bien moi truong nhu truoc.
            "SAMPLE_ROWS": "{{ params.sample_rows if params.sample_rows is not none"
                           " else (dag_run.conf.get('sample_rows') if dag_run else None)"
                           " or '' }}",
```

**Đơn giản hoá nếu biểu thức trên khó đọc:** Airflow đã nạp `conf` vào `params`, nên `{{ params.sample_rows or '' }}` là đủ. Dùng bản ngắn:

```python
            "SAMPLE_ROWS": "{{ params.sample_rows or '' }}",
```

`extract/main.py` đã xử lý chuỗi rỗng thành "dùng mọi dòng" từ Plan 2, nên không cần sửa stage.

- [ ] **Step 4: Kiểm DAG vẫn parse**

```powershell
.venv\Scripts\python.exe -c "import ast; ast.parse(open('dags/ml_pipeline_dag.py', encoding='utf-8').read()); print('parses')"
docker compose exec -T airflow-scheduler airflow dags list-import-errors
```

Expected: `parses`; không có import error.

- [ ] **Step 5: Chứng minh param thật sự tới được stage**

```powershell
docker compose exec -T airflow-scheduler airflow dags trigger ml_pipeline -c '{\"task_type\": \"regression\", \"sample_rows\": 1000}'
```

Đợi task `extract` xong, rồi kiểm **fingerprint** trong MLflow hoặc trong log của `extract` — nó phải khác fingerprint của lần chạy không giới hạn dòng.

```powershell
docker compose exec -T airflow-scheduler airflow tasks logs ml_pipeline extract <run_id> 1 2>&1 | Select-String "sample_rows"
```

Expected: log in ra `sample_rows=1000`, không phải `None`.

**Đây là bằng chứng thật của task này.** Dán vào report.

- [ ] **Step 6: Commit**

```bash
git add dags/ml_pipeline_dag.py
git commit -m "$(cat <<'EOF'
feat: sample_rows is a DAG param, not a scheduler environment variable

It used to be read from the scheduler's own environment, which froze it at
container start - the dashboard had no way to choose a row count, because
by the time a request arrived the value was already fixed.

No cache risk: compute_fingerprint has taken sample_rows since plan 1,
precisely so a 200k-row run can never reuse the split built from two
million. Changing the count produces a different fingerprint and the caches
separate themselves.

Verified by triggering with sample_rows=1000 and reading the value back out
of the extract stage's log.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: `clients/registry.py` + `routes/models.py` — metric động và promote

**Files:**
- Create: `services/api/clients/registry.py`, `services/api/routes/models.py`, `services/api/tests/test_models.py`
- Modify: `services/api/app.py`

**Interfaces:**
- Produces:
  - `RegistryClient(client=None)` với `.list_models() -> list[dict]`, `.promote(name, version) -> dict`
  - `GET /api/models`, `POST /api/models/{name}/{version}/promote`

- [ ] **Step 1: Viết test fail trước**

Tạo `services/api/tests/test_models.py`:

```python
from fastapi.testclient import TestClient

from services.api.app import create_app


class FakeRegistry:
    def __init__(self, models=None, promote_error=None):
        self.models = models if models is not None else []
        self.promote_error = promote_error
        self.promoted = []

    def list_models(self):
        return self.models

    def promote(self, name, version):
        if self.promote_error:
            raise self.promote_error
        self.promoted.append((name, version))
        return {"name": name, "version": version, "alias": "champion"}


def _client(registry):
    return TestClient(create_app(registry=registry))


REGRESSION = {
    "name": "house_price_regressor",
    "task_type": "regression",
    "versions": [
        {"version": "3", "metrics": {"rmse": 41203.7, "mae": 28104.2, "r2": 0.947},
         "is_champion": True, "created_at": "2026-09-19T10:00:00+00:00"},
    ],
}

CLASSIFICATION = {
    "name": "house_needs_renovation_classifier",
    "task_type": "classification",
    "versions": [
        {"version": "1", "metrics": {"auc": 0.71, "f1": 0.66, "accuracy": 0.68},
         "is_champion": True, "created_at": "2026-09-19T11:00:00+00:00"},
    ],
}


def test_models_returns_both_registered_models():
    body = _client(FakeRegistry([REGRESSION, CLASSIFICATION])).get("/api/models").json()
    assert [m["name"] for m in body["models"]] == [
        "house_price_regressor",
        "house_needs_renovation_classifier",
    ]


def test_metric_keys_differ_between_task_types():
    # Section 8.1 names hardcoding accuracy/f1 as a defect of the old frontend.
    # The API must report whichever keys the model actually has.
    body = _client(FakeRegistry([REGRESSION, CLASSIFICATION])).get("/api/models").json()
    by_name = {m["name"]: m for m in body["models"]}

    assert set(by_name["house_price_regressor"]["versions"][0]["metrics"]) == {
        "rmse", "mae", "r2",
    }
    assert set(by_name["house_needs_renovation_classifier"]["versions"][0]["metrics"]) == {
        "auc", "f1", "accuracy",
    }


def test_champion_is_flagged():
    body = _client(FakeRegistry([REGRESSION])).get("/api/models").json()
    assert body["models"][0]["versions"][0]["is_champion"] is True


def test_no_registered_models_is_an_empty_list_not_an_error():
    response = _client(FakeRegistry([])).get("/api/models")
    assert response.status_code == 200
    assert response.json()["models"] == []


def test_promote_moves_the_champion_alias():
    registry = FakeRegistry([REGRESSION])
    response = _client(registry).post("/api/models/house_price_regressor/4/promote")

    assert response.status_code == 200
    assert response.json()["alias"] == "champion"
    assert registry.promoted == [("house_price_regressor", "4")]


def test_promote_a_version_that_does_not_exist_is_404():
    registry = FakeRegistry([], promote_error=KeyError("no such version"))
    response = _client(registry).post("/api/models/house_price_regressor/99/promote")
    assert response.status_code == 404
```

- [ ] **Step 2: Chạy cho fail**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_models.py -v
```

Expected: FAIL — 404 trên mọi route.

- [ ] **Step 3: Implement client**

`services/api/clients/registry.py`:

```python
"""Reads the MLflow Model Registry and moves the champion alias.

Metrics are reported as whatever keys the run actually logged. Section 8.1
of the design doc names hardcoding accuracy and f1 as a defect of the old
frontend: regression has rmse, mae and r2, classification has auc, f1 and
accuracy, and a fixed list is wrong for one of them whichever list you pick.

Promotion moves an alias, never a stage. MLflow deprecated stages in 2.x and
removes them in 3.x.
"""

from __future__ import annotations

import os

CHAMPION_ALIAS = "champion"

MODEL_TASK_TYPES = {
    "house_price_regressor": "regression",
    "house_needs_renovation_classifier": "classification",
}


class RegistryClient:
    """Talks to the MLflow Model Registry.

    Example:
        client = RegistryClient()
        client.list_models()
        # -> [{"name": "house_price_regressor", "task_type": "regression",
        #      "versions": [{"version": "3", "metrics": {"rmse": ...},
        #                    "is_champion": True, "created_at": "..."}]}]
    """

    def __init__(self, client=None):
        """Opens a Model Registry client.

        Args:
            client: an MlflowClient, or anything with the same surface.
                None builds a real one from MLFLOW_TRACKING_URI. Injected so
                the tests need no MLflow.
        """
        if client is not None:
            self._client = client
        else:
            import mlflow
            from mlflow import MlflowClient

            mlflow.set_tracking_uri(os.environ["MLFLOW_TRACKING_URI"])
            self._client = MlflowClient()

    def list_models(self) -> list[dict]:
        """Lists every registered model with its versions and their metrics.

        Args:
            None.

        Returns:
            One entry per model with `name`, `task_type` and `versions`. Each
            version carries `version`, `metrics`, `is_champion` and
            `created_at`. `metrics` holds the keys that run actually logged,
            with the `test_` prefix the evaluate stage adds stripped off - so
            regression and classification legitimately differ.

        Example:
            list_models()
            # -> regression versions carry rmse/mae/r2,
            #    classification versions carry auc/f1/accuracy
        """
        result: list[dict] = []
        for registered in self._client.search_registered_models():
            champion_version = None
            try:
                champion_version = self._client.get_model_version_by_alias(
                    registered.name, CHAMPION_ALIAS
                ).version
            except Exception:  # noqa: BLE001 - no champion yet is a normal state
                champion_version = None

            versions = []
            for version in self._client.search_model_versions(f"name='{registered.name}'"):
                logged = self._client.get_run(version.run_id).data.metrics
                metrics = {
                    name[len("test_") :]: value
                    for name, value in logged.items()
                    if name.startswith("test_")
                }
                versions.append(
                    {
                        "version": str(version.version),
                        "metrics": metrics,
                        "is_champion": str(version.version) == str(champion_version),
                        "created_at": version.creation_timestamp,
                    }
                )
            result.append(
                {
                    "name": registered.name,
                    "task_type": MODEL_TASK_TYPES.get(registered.name),
                    "versions": sorted(versions, key=lambda v: int(v["version"]), reverse=True),
                }
            )
        return result

    def promote(self, name: str, version: str) -> dict:
        """Moves the champion alias onto one version.

        Args:
            name: the registered model.
            version: the version to promote.

        Returns:
            `name`, `version` and `alias`.

        Raises:
            Exception: when the model or version does not exist. The route
                turns that into a 404 rather than reporting a promotion that
                did not happen.

        Example:
            promote("house_price_regressor", "4")
            # -> {"name": "house_price_regressor", "version": "4",
            #     "alias": "champion"}
            # Older versions stay exactly as they were; nothing is archived.
        """
        self._client.set_registered_model_alias(name, CHAMPION_ALIAS, version)
        return {"name": name, "version": version, "alias": CHAMPION_ALIAS}
```

- [ ] **Step 4: Implement routes**

`services/api/routes/models.py`:

```python
"""Listing model versions and promoting one to champion."""

from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException, Request

from ..deps import require_auth

router = APIRouter()


@router.get("/models", dependencies=[Depends(require_auth)])
def list_models(request: Request) -> dict:
    """Lists registered models, their versions and their metrics.

    Args:
        request: the FastAPI request.

    Returns:
        `models`. Each version's `metrics` holds whichever keys that model
        logged - regression and classification differ, and the UI must read
        the keys rather than assume a fixed set.

    Example:
        # GET /api/models
        # {"models": [{"name": "house_price_regressor",
        #              "task_type": "regression",
        #              "versions": [{"version": "3",
        #                            "metrics": {"rmse": 41203.7, ...},
        #                            "is_champion": true}]}]}
    """
    return {"models": request.app.state.registry.list_models()}


@router.post("/models/{name}/{version}/promote", dependencies=[Depends(require_auth)])
def promote(request: Request, name: str, version: str) -> dict:
    """Moves the champion alias onto one version.

    Args:
        request: the FastAPI request.
        name: the registered model.
        version: the version to promote.

    Returns:
        `name`, `version` and `alias`.

    Raises:
        HTTPException: 404 when the model or version does not exist. This is
            a real write against the Registry, so the UI must confirm before
            calling it and must not report success on a failure.

    Example:
        # POST /api/models/house_price_regressor/4/promote
        # -> {"name": "house_price_regressor", "version": "4",
        #     "alias": "champion"}
    """
    try:
        return request.app.state.registry.promote(name, version)
    except Exception as err:  # noqa: BLE001 - surfaced as 404 with the reason
        raise HTTPException(status_code=404, detail=f"cannot promote: {err}") from err
```

Đăng ký router trong `app.py`.

- [ ] **Step 5: Chạy cho pass**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/ -q
.venv\Scripts\python.exe -m ruff check . --fix
```

- [ ] **Step 6: Commit**

```bash
git add services/api/clients/registry.py services/api/routes/models.py services/api/app.py services/api/tests/test_models.py
git commit -m "$(cat <<'EOF'
feat: model listing with real metric keys, and champion promotion

Metrics are reported as whatever the run logged rather than a fixed set.
Section 8.1 names hardcoding accuracy and f1 as a defect of the old
frontend, and it is wrong for one of the two task types whichever list you
pick: regression has rmse, mae and r2; classification has auc, f1 and
accuracy.

A model with no champion yet is a normal state, not an error - that branch
is unpromoted until it has been trained.

Promotion moves an alias, never a stage. MLflow deprecated stages in 2.x
and removes them in 3.x.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: `clients/reports.py` + `routes/drift.py`

**Files:**
- Create: `services/api/clients/reports.py`, `services/api/routes/drift.py`, `services/api/tests/test_drift.py`
- Modify: `services/api/app.py`

**Interfaces:**
- Consumes: `ml_common.storage.drift_latest_key`, `ml_common.storage.Storage`
- Produces: `ReportsClient(storage)` với `.latest(model_name) -> dict | None`, `.history(model_name, limit) -> list[dict]`; `GET /api/drift/latest`, `GET /api/drift/history`

- [ ] **Step 1: Viết test fail trước**

Tạo `services/api/tests/test_drift.py`:

```python
from fastapi.testclient import TestClient

from services.api.app import create_app
from services.api.clients.reports import ReportsClient

SUMMARY = {
    "model_name": "house_price_regressor",
    "model_version": "3",
    "task_type": "regression",
    "run_id": "20260920T075645",
    "computed_at": "2026-09-20T07:56:45+00:00",
    "severity": "high",
    "parts": {"feature": "ok", "prediction": "high", "performance": "high"},
    "n_predictions": 500,
    "n_ground_truth": 500,
    "current_metrics": {"rmse": 305791.8, "mae": 184188.3, "r2": 0.567},
    "report_key": "reports/house_price_regressor/20260920T075645/evidently.html",
}


class FakeStorage:
    def __init__(self, objects: dict):
        self._objects = objects

    def exists(self, key):
        return key in self._objects

    def read_json(self, key):
        return self._objects[key]

    def list_keys(self, prefix):
        return sorted(k for k in self._objects if k.startswith(prefix))


def _client(objects):
    return TestClient(create_app(reports=ReportsClient(FakeStorage(objects))))


def test_latest_returns_the_summary_plan_4_wrote():
    objects = {"reports/house_price_regressor/latest.json": SUMMARY}
    body = _client(objects).get("/api/drift/latest?model_name=house_price_regressor").json()

    assert body["severity"] == "high"
    assert body["parts"]["feature"] == "ok"
    assert body["parts"]["performance"] == "high"


def test_latest_before_monitoring_has_ever_run_is_404():
    response = _client({}).get("/api/drift/latest?model_name=house_price_regressor")
    assert response.status_code == 404


def test_history_returns_summaries_newest_first():
    objects = {
        "reports/house_price_regressor/20260920T0700/summary.json": {**SUMMARY, "run_id": "a"},
        "reports/house_price_regressor/20260920T0800/summary.json": {**SUMMARY, "run_id": "b"},
    }
    body = (
        _client(objects).get("/api/drift/history?model_name=house_price_regressor").json()
    )
    assert [item["run_id"] for item in body["history"]] == ["b", "a"]


def test_history_ignores_objects_that_are_not_summaries():
    objects = {
        "reports/house_price_regressor/latest.json": SUMMARY,
        "reports/house_price_regressor/20260920T0800/summary.json": SUMMARY,
    }
    client = ReportsClient(FakeStorage(objects))
    assert len(client.history("house_price_regressor", limit=10)) == 1


def test_history_honours_the_limit():
    objects = {
        f"reports/house_price_regressor/run{i}/summary.json": {**SUMMARY, "run_id": str(i)}
        for i in range(5)
    }
    client = ReportsClient(FakeStorage(objects))
    assert len(client.history("house_price_regressor", limit=2)) == 2


def test_history_with_no_runs_is_an_empty_list_not_an_error():
    response = _client({}).get("/api/drift/history?model_name=house_price_regressor")
    assert response.status_code == 200
    assert response.json()["history"] == []
```

- [ ] **Step 2: Chạy cho fail**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_drift.py -v
```

Expected: FAIL với `ModuleNotFoundError: services.api.clients.reports`

- [ ] **Step 3: Implement**

`services/api/clients/reports.py`:

```python
"""Reads the drift verdicts the monitor stage wrote to object storage.

Plan 4 writes two things per run: a full summary under the run's own prefix,
and a copy at a fixed `latest.json`. Reading the fixed key is one request;
finding the newest by listing and comparing timestamps would be many.
"""

from __future__ import annotations

from ml_common.storage import drift_latest_key

SUMMARY_SUFFIX = "/summary.json"


class ReportsClient:
    """Reads drift summaries for the dashboard.

    Example:
        client = ReportsClient(Storage.from_env())
        client.latest("house_price_regressor")
        # -> {"severity": "high",
        #     "parts": {"feature": "ok", "prediction": "high",
        #               "performance": "high"}, ...}
    """

    def __init__(self, storage):
        """Records where the reports live.

        Args:
            storage: a `Storage`, or anything with `exists`, `read_json` and
                `list_keys`. Injected so the tests need no MinIO.
        """
        self._storage = storage

    def latest(self, model_name: str) -> dict | None:
        """Reads the most recent drift verdict for one model.

        Args:
            model_name: the registered model.

        Returns:
            The summary the monitor stage wrote, or None when monitoring has
            never run for this model. None is a normal early state, not an
            error - the route turns it into a 404 so the UI can show an empty
            state rather than an alarming one.

        Example:
            latest("house_price_regressor")
            # -> {"severity": "high", "parts": {...}, "n_ground_truth": 500,
            #     "report_key": "reports/.../evidently.html"}
        """
        key = drift_latest_key(model_name)
        if not self._storage.exists(key):
            return None
        return self._storage.read_json(key)

    def history(self, model_name: str, limit: int) -> list[dict]:
        """Reads recent drift verdicts, newest first.

        Args:
            model_name: the registered model.
            limit: how many verdicts to return.

        Returns:
            Summaries ordered newest first. Only objects ending in
            "/summary.json" are read - the same prefix also holds
            `latest.json` and Evidently's own HTML and JSON, and folding
            those in would produce nonsense rather than an error.

        Example:
            history("house_price_regressor", limit=20)
            # -> [{"run_id": "20260920T0800", "severity": "warning", ...},
            #     {"run_id": "20260920T0700", "severity": "ok", ...}]
        """
        prefix = f"reports/{model_name}/"
        keys = [key for key in self._storage.list_keys(prefix) if key.endswith(SUMMARY_SUFFIX)]
        keys.sort(reverse=True)
        return [self._storage.read_json(key) for key in keys[:limit]]
```

`services/api/routes/drift.py`:

```python
"""Serving the drift verdicts to the dashboard."""

from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException, Request

from ..deps import require_auth

router = APIRouter()

DEFAULT_HISTORY_LIMIT = 20


@router.get("/drift/latest", dependencies=[Depends(require_auth)])
def latest(request: Request, model_name: str) -> dict:
    """Returns the most recent drift verdict for one model.

    Args:
        request: the FastAPI request.
        model_name: which registered model.

    Returns:
        The summary as the monitor stage wrote it, including `severity`, the
        three `parts`, and `report_key`. Note that `parts.performance` can be
        `insufficient_data`, which is NOT the same as `ok` - ground truth
        arrives after the prediction it describes, so early on there is
        simply nothing to measure.

    Raises:
        HTTPException: 404 when monitoring has never run for this model. The
            UI shows an empty state for that, not an error.

    Example:
        # GET /api/drift/latest?model_name=house_price_regressor
        # {"severity": "high",
        #  "parts": {"feature": "ok", "prediction": "high",
        #            "performance": "high"}, ...}
    """
    summary = request.app.state.reports.latest(model_name)
    if summary is None:
        raise HTTPException(status_code=404, detail=f"no drift report yet for {model_name}")
    return summary


@router.get("/drift/history", dependencies=[Depends(require_auth)])
def history(request: Request, model_name: str, limit: int = DEFAULT_HISTORY_LIMIT) -> dict:
    """Returns recent drift verdicts, newest first.

    Args:
        request: the FastAPI request.
        model_name: which registered model.
        limit: how many verdicts to return.

    Returns:
        `history`. An empty list when monitoring has never run - unlike
        `latest`, absence here is not an error, because "no history" is a
        chart with no points rather than a missing page.

    Example:
        # GET /api/drift/history?model_name=house_price_regressor&limit=5
        # {"history": [{"run_id": "...", "severity": "warning", ...}]}
    """
    return {"history": request.app.state.reports.history(model_name, limit)}
```

Đăng ký router trong `app.py`.

- [ ] **Step 4: Chạy cho pass**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/ -q
.venv\Scripts\python.exe -m ruff check . --fix
```

- [ ] **Step 5: Commit**

```bash
git add services/api/clients/reports.py services/api/routes/drift.py services/api/app.py services/api/tests/test_drift.py
git commit -m "$(cat <<'EOF'
feat: drift endpoints reading what plan 4 wrote

latest reads one fixed key, which is why plan 4 overwrites latest.json on
every run: finding the newest by listing a prefix and comparing timestamps
would be many requests to answer a question that has one.

history reads only objects ending in /summary.json. The same prefix holds
latest.json and Evidently's own HTML and JSON, and folding those in would
produce nonsense rather than an error.

A missing latest is a 404 and a missing history is an empty list. They are
different questions: one is a page with nothing on it, the other is a chart
with no points.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: `POST /api/data/upload` — đọc theo luồng, có trần

**Files:**
- Create: `services/api/routes/data.py`, `services/api/tests/test_data.py`
- Modify: `services/api/app.py`

**Interfaces:**
- Consumes: `ml_common.storage.Storage.upload_file`, `ml_common.storage.raw_key`
- Produces: `POST /api/data/upload` → `{"dataset_version", "rows", "size_mb"}`

- [ ] **Step 1: Viết test fail trước**

Tạo `services/api/tests/test_data.py`:

```python
import io

from fastapi.testclient import TestClient

from services.api.app import create_app

CSV = b"property_id,city,sale_price\np1,boston,450000\np2,miami,380000\n"


class FakeStorage:
    def __init__(self):
        self.uploaded = []

    def upload_file(self, local_path, key):
        with open(local_path, "rb") as handle:
            self.uploaded.append((key, handle.read()))

    def exists(self, key):
        return any(k == key for k, _ in self.uploaded)


def _client(storage, max_bytes=None):
    app = create_app(storage=storage)
    if max_bytes is not None:
        app.state.max_upload_bytes = max_bytes
    return TestClient(app)


def test_upload_stores_the_file_under_the_dataset_version():
    storage = FakeStorage()
    response = _client(storage).post(
        "/api/data/upload",
        files={"file": ("data.csv", io.BytesIO(CSV), "text/csv")},
        data={"dataset_version": "v2"},
    )

    assert response.status_code == 200
    assert response.json()["dataset_version"] == "v2"
    key, content = storage.uploaded[0]
    assert key == "raw/v2/data.parquet"
    assert len(content) > 0


def test_upload_reports_the_row_count():
    body = _client(FakeStorage()).post(
        "/api/data/upload",
        files={"file": ("data.csv", io.BytesIO(CSV), "text/csv")},
        data={"dataset_version": "v2"},
    ).json()
    assert body["rows"] == 2


def test_upload_over_the_cap_is_413_not_an_out_of_memory_kill():
    big = b"a,b\n" + b"1,2\n" * 10_000
    response = _client(FakeStorage(), max_bytes=100).post(
        "/api/data/upload",
        files={"file": ("data.csv", io.BytesIO(big), "text/csv")},
        data={"dataset_version": "v2"},
    )
    assert response.status_code == 413


def test_a_rejected_upload_stores_nothing():
    storage = FakeStorage()
    big = b"a,b\n" + b"1,2\n" * 10_000
    _client(storage, max_bytes=100).post(
        "/api/data/upload",
        files={"file": ("data.csv", io.BytesIO(big), "text/csv")},
        data={"dataset_version": "v2"},
    )
    assert storage.uploaded == []


def test_upload_rejects_a_file_that_is_not_csv():
    response = _client(FakeStorage()).post(
        "/api/data/upload",
        files={"file": ("data.txt", io.BytesIO(b"not csv"), "text/plain")},
        data={"dataset_version": "v2"},
    )
    assert response.status_code == 422
```

- [ ] **Step 2: Chạy cho fail**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_data.py -v
```

Expected: FAIL — `create_app() got an unexpected keyword argument 'storage'`

- [ ] **Step 3: Implement**

Thêm `storage=None` vào chữ ký `create_app`, lưu vào `app.state.storage`, và thêm `app.state.max_upload_bytes = 500 * 1024 * 1024`.

`services/api/routes/data.py`:

```python
"""Uploading a raw dataset and previewing what is in it.

The source CSV is hundreds of megabytes and the machine running this has
16GB of RAM and seven other containers on it. Nothing here reads a whole
upload into memory: it streams to a temporary file, converts, uploads with
`Storage.upload_file`, and deletes the temporary file on every path.
"""

from __future__ import annotations

import os
import tempfile

import pandas as pd
from fastapi import APIRouter, Depends, File, Form, HTTPException, Request, UploadFile

from ml_common.storage import raw_key

from ..deps import require_auth

router = APIRouter()

CHUNK_BYTES = 1024 * 1024


@router.post("/data/upload", dependencies=[Depends(require_auth)])
async def upload(
    request: Request,
    file: UploadFile = File(...),
    dataset_version: str = Form(...),
) -> dict:
    """Streams a CSV to object storage as the raw dataset of a version.

    Args:
        request: the FastAPI request.
        file: the uploaded CSV.
        dataset_version: which version to write under, e.g. "v2".

    Returns:
        `dataset_version`, `rows` and `size_mb`.

    Raises:
        HTTPException: 422 when the file is not a CSV, 413 when it exceeds
            the cap. The cap exists because this machine has 16GB of RAM and
            an out-of-memory kill takes the whole stack with it, which is a
            far worse outcome than a refused upload.

    Example:
        # POST /api/data/upload  (multipart: file=..., dataset_version=v2)
        # -> {"dataset_version": "v2", "rows": 2000000, "size_mb": 118.4}
    """
    if not file.filename or not file.filename.lower().endswith(".csv"):
        raise HTTPException(status_code=422, detail="only .csv uploads are accepted")

    max_bytes = request.app.state.max_upload_bytes
    workdir = tempfile.mkdtemp()
    csv_path = os.path.join(workdir, "upload.csv")
    parquet_path = os.path.join(workdir, "data.parquet")
    written = 0
    try:
        with open(csv_path, "wb") as handle:
            while True:
                chunk = await file.read(CHUNK_BYTES)
                if not chunk:
                    break
                written += len(chunk)
                if written > max_bytes:
                    raise HTTPException(
                        status_code=413,
                        detail=f"upload exceeds the {max_bytes} byte limit",
                    )
                handle.write(chunk)

        # Every column is read as text on purpose: this is the RAW copy, and
        # parsing belongs to the Pipeline. Letting pandas infer types here
        # would quietly repair some of the dirt the pipeline exists to handle.
        frame = pd.read_csv(csv_path, dtype=str, keep_default_na=False)
        frame.to_parquet(parquet_path, index=False, compression="snappy")
        request.app.state.storage.upload_file(parquet_path, raw_key(dataset_version))
        return {
            "dataset_version": dataset_version,
            "rows": int(len(frame)),
            "size_mb": round(os.path.getsize(parquet_path) / 2**20, 2),
        }
    finally:
        for path in (csv_path, parquet_path):
            if os.path.exists(path):
                os.remove(path)
        os.rmdir(workdir)
```

- [ ] **Step 4: Chạy cho pass**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_data.py -v
.venv\Scripts\python.exe -m ruff check . --fix
```

Expected: 5 passed.

- [ ] **Step 5: Commit**

```bash
git add services/api/routes/data.py services/api/app.py services/api/tests/test_data.py
git commit -m "$(cat <<'EOF'
feat: streaming CSV upload with a size cap

Nothing reads the whole upload into memory. It streams to a temporary file
a megabyte at a time, converts, uploads through Storage.upload_file - which
exists for exactly this reason - and removes the temporary files on every
path, including the rejected one.

The cap returns 413 rather than letting the machine decide. This box has
16GB and seven other containers; an out-of-memory kill takes the whole
stack down, which is a far worse outcome than a refused upload.

Columns are read as text, as seed_raw_data.py does. Letting pandas infer
types would quietly repair some of the dirt the pipeline exists to handle.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: `GET /api/data/{dataset_version}/preview`

**Files:**
- Modify: `services/api/routes/data.py`, `services/api/tests/test_data.py`

**Interfaces:**
- Consumes: `ml_common.validation.validate_dataframe`, `ml_common.storage.raw_key`
- Produces: `GET /api/data/{dataset_version}/preview?rows=50`

- [ ] **Step 1: Viết test fail trước**

Thêm vào `services/api/tests/test_data.py`:

```python
import pandas as pd


class FakeStorageWithRead(FakeStorage):
    def __init__(self, frame):
        super().__init__()
        self._frame = frame

    def exists(self, key):
        return True

    def read_parquet(self, key):
        return self._frame


def _preview_client(frame):
    return TestClient(create_app(storage=FakeStorageWithRead(frame)))


PREVIEW_FRAME = pd.DataFrame(
    {
        "property_id": ["p1", "p2", "p3"],
        "city": ["boston", "miami", None],
        "bedrooms": ["3", "4", "999"],
        "sale_price": ["$450,000", "$380,000", "$1"],
    }
)


def test_preview_returns_sample_rows_and_column_stats():
    body = _preview_client(PREVIEW_FRAME).get("/api/data/v1/preview?rows=2").json()

    assert body["total_rows"] == 3
    assert len(body["sample"]) == 2
    names = [column["name"] for column in body["columns"]]
    assert "bedrooms" in names


def test_preview_reports_missing_rate_from_ml_common_not_its_own_count():
    body = _preview_client(PREVIEW_FRAME).get("/api/data/v1/preview").json()
    city = next(c for c in body["columns"] if c["name"] == "city")
    assert city["missing_rate"] > 0


def test_preview_reports_out_of_bounds_counts():
    # bedrooms is declared max_value=20 in the schema; "999" is outside it.
    body = _preview_client(PREVIEW_FRAME).get("/api/data/v1/preview").json()
    bedrooms = next(c for c in body["columns"] if c["name"] == "bedrooms")
    assert bedrooms["out_of_bounds"] == 1


def test_preview_of_a_version_that_does_not_exist_is_404():
    class Missing(FakeStorage):
        def exists(self, key):
            return False

    client = TestClient(create_app(storage=Missing()))
    assert client.get("/api/data/v9/preview").status_code == 404


def test_preview_rows_query_is_capped():
    body = _preview_client(PREVIEW_FRAME).get("/api/data/v1/preview?rows=10000").json()
    assert len(body["sample"]) <= 3
```

- [ ] **Step 2: Chạy cho fail**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/test_data.py -k preview -v
```

Expected: FAIL — 404, route chưa tồn tại.

- [ ] **Step 3: Implement**

Thêm vào `services/api/routes/data.py`:

```python
from ml_common import schema
from ml_common.validation import validate_dataframe

MAX_PREVIEW_ROWS = 200
DEFAULT_PREVIEW_ROWS = 50


@router.get("/data/{dataset_version}/preview", dependencies=[Depends(require_auth)])
def preview(request: Request, dataset_version: str, rows: int = DEFAULT_PREVIEW_ROWS) -> dict:
    """Shows a sample of a raw dataset and the state of each column.

    Args:
        request: the FastAPI request.
        dataset_version: which version to read, e.g. "v1".
        rows: how many sample rows to show, capped at MAX_PREVIEW_ROWS.
            This is ONLY the preview size; it has nothing to do with the
            `sample_rows` that limits a training run, and the UI must not
            let the two look like the same control.

    Returns:
        `total_rows`, `sample` (a list of row dicts) and `columns` - each
        with `name`, `kind`, `missing_rate` and `out_of_bounds`. The stats
        come from `ml_common.validation`, the same code the validate stage
        runs, so the dashboard and the pipeline can never disagree about how
        dirty the data is.

    Raises:
        HTTPException: 404 when that dataset version has not been uploaded.

    Example:
        # GET /api/data/v1/preview?rows=50
        # {"total_rows": 2000000,
        #  "sample": [{"property_id": "p1", "city": "  NEW YORK ", ...}],
        #  "columns": [{"name": "bedrooms", "kind": "numeric",
        #               "missing_rate": 0.031, "out_of_bounds": 87}]}
    """
    key = raw_key(dataset_version)
    if not request.app.state.storage.exists(key):
        raise HTTPException(status_code=404, detail=f"no dataset at version {dataset_version}")

    frame = request.app.state.storage.read_parquet(key)
    report = validate_dataframe(frame, "regression")
    wanted = max(1, min(rows, MAX_PREVIEW_ROWS))

    columns = [
        {
            "name": name,
            "kind": schema.COLUMNS[name].kind if name in schema.COLUMNS else "unknown",
            "missing_rate": counts["missing_rate"],
            "out_of_bounds": counts["out_of_bounds"],
        }
        for name, counts in sorted(report["columns"].items())
    ]
    return {
        "total_rows": report["row_count"],
        "sample": frame.head(wanted).to_dict(orient="records"),
        "columns": columns,
    }
```

- [ ] **Step 4: Chạy cho pass**

```powershell
.venv\Scripts\python.exe -m pytest services/api/tests/ -q
.venv\Scripts\python.exe -m ruff check . --fix
```

- [ ] **Step 5: Commit**

```bash
git add services/api/routes/data.py services/api/tests/test_data.py
git commit -m "$(cat <<'EOF'
feat: dataset preview with column stats from ml_common

Missing rates and out-of-bounds counts come from validate_dataframe, the
same code the validate stage runs. A second implementation here would be a
second answer to "how dirty is this data", and the two would drift.

The rows query caps the preview only. It has nothing to do with the
sample_rows that limits a training run, and the UI must not let the two
look like the same control - one decides what you are looking at, the
other decides what the model learns from.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: Nối client thật, probe sức khoẻ, và `verify_api.ps1`

**Files:**
- Modify: `services/api/app.py`
- Create: `scripts/verify_api.ps1`

**Interfaces:**
- Produces: `create_app()` không tham số dựng client thật; `GET /api/health` báo 5 phụ thuộc

- [ ] **Step 1: Dựng client thật khi không được inject**

Thêm import vào đầu `services/api/app.py` — `os` và `httpx` cho các probe, và ba client:

```python
import os

import httpx

from .clients.airflow import AirflowClient
from .clients.registry import RegistryClient
from .clients.reports import ReportsClient
from ml_common.storage import Storage
```

Sửa `create_app` trong `services/api/app.py`:

```python
    app.state.airflow = airflow if airflow is not None else _real_airflow()
    app.state.registry = registry if registry is not None else _real_registry()
    app.state.storage = storage if storage is not None else _real_storage()
    app.state.reports = reports if reports is not None else ReportsClient(app.state.storage)
    app.state.probes = probes if probes is not None else _real_probes(app.state)
```

Thêm các hàm dựng, mỗi hàm **bắt lỗi và trả None** để API vẫn khởi động được khi một phụ thuộc chưa lên — `/health` sẽ báo `down` thay vì container chết trong vòng lặp restart:

```python
def _real_airflow():
    """Builds the Airflow client from the environment, or None if it cannot.

    Args:
        None. Reads AIRFLOW_API_URL, AIRFLOW_USERNAME, AIRFLOW_PASSWORD.

    Returns:
        An `AirflowClient`, or None when the variables are absent. None means
        /health reports airflow as down instead of the container dying at
        startup and restarting forever - a dashboard that loads and says what
        is broken beats one that never loads.

    Example:
        _real_airflow()  # -> AirflowClient(...) or None
    """
    try:
        return AirflowClient(
            os.environ["AIRFLOW_API_URL"],
            os.environ["AIRFLOW_USERNAME"],
            os.environ["AIRFLOW_PASSWORD"],
        )
    except KeyError:
        return None
```

Viết `_real_registry()` và `_real_storage()` theo cùng khuôn, và `_real_probes(state)` trả dict 5 probe:

```python
def _real_probes(state) -> dict:
    """Builds one health probe per dependency.

    Args:
        state: the app state holding the already-built clients.

    Returns:
        Name to a zero-argument callable. Each probe makes the cheapest call
        that proves the dependency answers - listing one DAG, one registered
        model, one object - not a full query.

    Example:
        _real_probes(app.state)
        # -> {"airflow": <callable>, "mlflow": <callable>, "minio": <callable>,
        #     "serving": <callable>, "postgres": <callable>}
    """
    # Each probe returns True when the call COMPLETED. The value it returns is
    # irrelevant - an empty run list is a healthy Airflow with nothing to show.
    # build_health() turns any raised exception into "down", so these only have
    # to make the call and not defend against failure themselves.
    def airflow_up() -> bool:
        if state.airflow is None:
            return False
        state.airflow.list_runs("ml_pipeline", 1)
        return True

    def mlflow_up() -> bool:
        if state.registry is None:
            return False
        state.registry.list_models()
        return True

    def minio_up() -> bool:
        if state.storage is None:
            return False
        state.storage.list_keys("reports/")
        return True

    def serving_up() -> bool:
        url = os.environ.get("SERVING_URL", "http://serving:8000")
        return httpx.get(f"{url}/health", timeout=5.0).status_code == 200

    def postgres_up() -> bool:
        # Airflow answering at all means its metadata database is reachable;
        # there is no separate probe worth the extra dependency.
        return airflow_up()

    return {
        "airflow": airflow_up,
        "mlflow": mlflow_up,
        "minio": minio_up,
        "serving": serving_up,
        "postgres": postgres_up,
    }
```

- [ ] **Step 2: Build lại và chạy**

```powershell
docker build -f services/api/Dockerfile -t ml-api:latest .
docker compose up -d --force-recreate api
curl.exe -s http://localhost:8001/api/health
```

Expected: JSON có đủ 5 service, phần lớn `ok`.

- [ ] **Step 3: Viết `scripts/verify_api.ps1`**

```powershell
# Xac nhan Plan 5a. Chay tu goc repo, voi stack dang chay.
$ErrorActionPreference = "Stop"
$api = "http://localhost:8001/api"

Write-Host "== 1/7 Test o may dev ==" -ForegroundColor Cyan
.venv\Scripts\python.exe -m pytest common/ services/ -q
if ($LASTEXITCODE -ne 0) { throw "test that bai" }

Write-Host "== 2/7 Ruff ==" -ForegroundColor Cyan
.venv\Scripts\python.exe -m ruff check .
if ($LASTEXITCODE -ne 0) { throw "ruff that bai" }

Write-Host "== 3/7 Airflow REST nhan basic auth ==" -ForegroundColor Cyan
$code = curl.exe -s -o NUL -w "%{http_code}" -u admin:admin http://localhost:8080/api/v1/dags
if ($code -ne "200") { throw "Airflow REST tra $code - kiem AIRFLOW__API__AUTH_BACKENDS" }

Write-Host "== 4/7 /api/health ==" -ForegroundColor Cyan
$health = curl.exe -s "$api/health" | ConvertFrom-Json
if (-not $health.services) { throw "/health khong tra services" }
$health.services | ConvertTo-Json -Compress | Write-Host

Write-Host "== 5/7 /api/models tra metric DONG theo task_type ==" -ForegroundColor Cyan
$models = curl.exe -s "$api/models" | ConvertFrom-Json
foreach ($m in $models.models) {
    if ($m.versions.Count -gt 0) {
        $keys = $m.versions[0].metrics.PSObject.Properties.Name -join ","
        Write-Host "   $($m.name) [$($m.task_type)]: $keys"
    }
}

Write-Host "== 6/7 /api/drift/latest ==" -ForegroundColor Cyan
$code = curl.exe -s -o NUL -w "%{http_code}" "$api/drift/latest?model_name=house_price_regressor"
if ($code -ne "200" -and $code -ne "404") { throw "/drift/latest tra $code" }
Write-Host "   HTTP $code (404 la hop le neu chua chay monitoring)"

Write-Host "== 7/7 Dashboard chua ton tai - khong co gi goi thang Airflow/MLflow ==" -ForegroundColor Cyan
Write-Host "   (Plan 5b se them buoc grep nay)"

Write-Host "`nPlan 5a xanh." -ForegroundColor Green
```

- [ ] **Step 4: Chạy script**

```powershell
powershell -ExecutionPolicy Bypass -File scripts\verify_api.ps1
```

Expected: xanh cả 7 bước.

- [ ] **Step 5: Commit**

```bash
git add services/api/app.py scripts/verify_api.ps1
git commit -m "$(cat <<'EOF'
feat: real clients, health probes, and a verification script

Each client builder returns None when its configuration is missing rather
than raising. The API then starts, and /health says which dependency is
down. A container that dies at startup and restarts forever tells you less
than a dashboard that loads and names the problem.

Probes make the cheapest call that proves a dependency answers - one DAG,
one model, one prefix - not a full query, because /health gets polled.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: Chứng minh Definition of Done trên hạ tầng thật

Task này **không viết code**. Nó đo.

**Files:**
- Không sửa file nào. Kết quả đi vào report.

- [ ] **Step 1: `sample_rows` thật sự có tác dụng (DoD điểm 5)**

```powershell
curl.exe -s -X POST "http://localhost:8001/api/pipeline/run" -H "Content-Type: application/json" -d '{\"task_type\":\"regression\",\"sample_rows\":1000}'
```

Lấy `run_id` trả về, đợi task `extract` xong, rồi lấy fingerprint:

```powershell
docker compose exec -T airflow-scheduler airflow tasks logs ml_pipeline extract <run_id> 1 2>&1 | Select-String "sample_rows|fingerprint"
```

Expected: `sample_rows=1000`, và fingerprint **khác** fingerprint của lần chạy không giới hạn.

**Nếu fingerprint giống nhau thì `sample_rows` không tới được stage** — dừng và báo, đừng ghi là đạt.

- [ ] **Step 2: Metric khác nhau giữa hai task type (DoD điểm 7)**

```powershell
curl.exe -s "http://localhost:8001/api/models" | ConvertFrom-Json | ForEach-Object { $_.models } | ForEach-Object { "$($_.name): $($_.versions[0].metrics.PSObject.Properties.Name -join ',')" }
```

Expected: regression in ra `rmse,mae,r2`; classification in ra `auc,f1,accuracy`. **Hai bộ khoá phải khác nhau.**

- [ ] **Step 3: promote đổi được alias (DoD điểm 8)**

Ghi lại version champion hiện tại, promote sang version khác, rồi `GET /api/models` xem `is_champion` đã dịch chưa. Promote **trả lại version cũ** sau khi kiểm xong, để không để hệ thống ở trạng thái lạ.

- [ ] **Step 4: drift/latest khớp file Plan 4 ghi (DoD điểm 6)**

```powershell
curl.exe -s "http://localhost:8001/api/drift/latest?model_name=house_price_regressor"
```

So với `summary.json` trong MinIO — phải giống hệt.

- [ ] **Step 5: Ghi lại toàn bộ phản hồi JSON thật**

Lưu phản hồi thật của **cả 11 endpoint** vào report. Task 13 sẽ trích chúng vào UI brief, nên chúng phải là JSON thật chứ không phải hình dạng ai đó tưởng tượng.

- [ ] **Step 6: Commit**

Không có thay đổi code. Nếu không có gì để commit thì nói rõ trong report và bỏ qua bước này.

---

## Task 13: UI design brief cho Claude design

Đây là deliverable người dùng yêu cầu. Nó phải **trích JSON thật từ Task 12**, không phải hình dạng đoán ra.

**Files:**
- Create: `docs/superpowers/specs/2026-09-20-plan5b-ui-brief.md`

- [ ] **Step 1: Viết brief**

Tạo `docs/superpowers/specs/2026-09-20-plan5b-ui-brief.md` với cấu trúc sau. Mỗi màn hình phải có: mục đích, component, **phản hồi API thật**, và mọi trạng thái.

Nội dung bắt buộc:

**Mở đầu** — một đoạn nói rõ đây là brief cho Claude design, dashboard là một file HTML tự chứa (React + Tailwind qua CDN, không build step), và nó **chỉ** gọi `http://localhost:8001/api`.

**Nguyên tắc thiết kế** — ghi đúng ba điều này, vì chúng là bài học đo được của Plan 4:

1. `insufficient_data` **phải trông khác** `ok`. Chưa đo được không phải là ổn. Đề xuất: xám có gạch chéo, kèm chữ "chưa đủ dữ liệu", không dùng màu xanh.
2. Ba loại drift **hiện riêng**, không gộp thành một badge. Plan 4 đo được `market_rally`: feature `ok` trong khi performance `high`. Một badge tổng hợp xanh ở đó sẽ nói dối.
3. Mọi màn hình cần **empty state** riêng. Ngày đầu chưa có traffic thì mọi màn đều trống, và "trống" phải khác "hỏng".

**Bảng màu trạng thái** — 4 trạng thái, không phải 3: `ok`, `warning`, `high`, `insufficient_data`.

**Năm màn hình**, mỗi màn hình một mục, gồm:
- Mục đích một câu
- Danh sách component (lấy từ spec mục 4)
- **Phản hồi JSON thật** dán từ Task 12
- Trạng thái: loading, empty, error, có dữ liệu
- Thao tác ghi nào cần `ConfirmDialog`

**Ràng buộc kỹ thuật** — một file HTML, React + Tailwind qua CDN, Chart.js cho biểu đồ, không build step, không gọi thẳng Airflow/MLflow/MinIO.

**Những thứ cố ý KHÔNG có** — realtime log streaming, auth, Feature Store.

- [ ] **Step 2: Kiểm mỗi component có endpoint nuôi**

Đọc lại brief và đối chiếu: mỗi component phải chỉ được ra endpoint cấp dữ liệu cho nó, và mỗi endpoint phải có ít nhất một component dùng. Ghi kết quả đối chiếu vào report — nếu lệch thì một trong hai chỗ sai.

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/specs/2026-09-20-plan5b-ui-brief.md
git commit -m "$(cat <<'EOF'
docs: UI design brief built from real API responses

Every screen carries the actual JSON the endpoints returned, captured after
the API was running against live infrastructure, so the design is drawn
against what exists rather than a shape someone imagined.

Three rules come from plan 4's measurements rather than from taste.
insufficient_data must not look like ok, because ground truth arrives late
and a green badge there claims a check that never happened. The three drift
types stay separate, because market_rally measured feature ok alongside
performance high and one combined badge would have been a lie. And every
screen needs its own empty state, because on day one they are all empty and
empty is not broken.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 14: Cập nhật tài liệu

**Files:**
- Modify: `CLAUDE.md`, `mlops-pipeline-design.md`

- [ ] **Step 1: `CLAUDE.md`**

Thêm vào khối build: `docker build -f services/api/Dockerfile -t ml-api:latest .`
Thêm vào danh sách verify: `powershell -ExecutionPolicy Bypass -File scripts\verify_api.ps1`
Thêm dòng vào bảng trạng thái cho plan 5a. Cập nhật số test (đo, đừng đoán).
Thêm `http://localhost:8001` vào bảng Giao diện.

- [ ] **Step 2: Vá ngược 4 chỗ vào `mlops-pipeline-design.md`**

Theo bảng ở mục 9 của spec Plan 5a:
1. §8.3 — `POST /api/pipeline/run` nhận thêm `sample_rows` và `dataset_version`.
2. §8.3 — ghi chú Airflow REST cần `AIRFLOW__API__AUTH_BACKENDS`.
3. §8.1 — màn Drift có **bốn** trạng thái và tách ba loại drift.
4. §3.2 — auth hoãn, nhưng có sẵn dependency rỗng.

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md mlops-pipeline-design.md
git commit -m "$(cat <<'EOF'
docs: record what plan 5a changed about the design

Four patches back into the design doc: the run endpoint takes sample_rows
and dataset_version, Airflow's REST API needs a basic_auth backend enabled
before any service can call it, the drift screen has four states rather
than three, and auth is deferred behind a dependency that exists but does
nothing.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Definition of Done

1. `pytest common/ services/ -q` xanh ở cả 3.13 (máy) lẫn 3.12 (container).
2. `ruff check .` sạch.
3. `ml-api:latest` build được; service `api` lên trong compose.
4. **`curl -u admin:admin http://localhost:8080/api/v1/dags` trả 200** (trước plan này là 401).
5. `POST /api/pipeline/run` với `sample_rows=1000` khởi động DAG run thật, và run đó **thực sự dùng 1000 dòng** — kiểm bằng fingerprint, không bằng lời.
6. `GET /api/drift/latest` trả đúng nội dung `summary.json` Plan 4 ghi.
7. `GET /api/models` trả `rmse`/`mae`/`r2` cho regression và `auc`/`f1`/`accuracy` cho classification — **khoá khác nhau**.
8. `POST /api/models/{name}/{version}/promote` chuyển được alias, `GET /api/models` phản ánh ngay sau đó.
9. `scripts\verify_api.ps1` xanh.
10. `docs/superpowers/specs/2026-09-20-plan5b-ui-brief.md` tồn tại, và mọi ví dụ JSON trong đó là **phản hồi thật**, không phải bịa.

Điểm 5 và 7 là chỗ plan này chứng minh mình có giá trị. Một API trả dữ liệu tĩnh trông giống hệt một API trả dữ liệu thật, cho tới khi bạn đổi tham số và kiểm xem phía sau có đổi theo không.

---

## Những gì Plan 5a cố ý KHÔNG làm

- **`dashboard/`.** Plan 5b.
- **Auth bằng API key.** Mục 3.2 nói chưa cần khi chạy local; dependency rỗng đã sẵn.
- **Realtime log streaming.** Đã chốt: log lấy khi bấm xem.
- **Sửa `services/serving/`, `stages/`, `common/ml_common/`.** Ngoại lệ duy nhất là `dags/ml_pipeline_dag.py` ở Task 6.
- **Auto-retrain.** §6.2 chốt từ Plan 4: cảnh báo, người quyết định.

---

## Self-review

**Spec coverage:**

| Mục spec | Task |
| --- | --- |
| 2.1 Airflow basic auth | Task 1 |
| 2.2 inject collaborator | Task 2 (khung), 3/7/8 (client) |
| 2.3 `sample_rows` thành param | Task 6 |
| 2.4 upload theo luồng, có trần | Task 9 |
| 2.5 `require_auth()` rỗng | Task 2 |
| 2.6 dashboard chỉ gọi API layer | Task 11 (verify), Plan 5b (grep) |
| 3 pipeline (4 endpoint) | Task 4, 5 |
| 3 dữ liệu (2 endpoint) | Task 9, 10 |
| 3 models (2 endpoint) | Task 7 |
| 3 drift (2 endpoint) | Task 8 |
| 3 health | Task 2, 11 |
| 4 danh sách component | Task 13 |
| 5 cấu trúc thư mục | Task 2–10 |
| 6 thay đổi code đã có | Task 1, 2, 6 |
| 7 test | Task 2–10 |
| 8 Definition of Done | Task 12 |
| 9 chỗ lệch spec | Task 14 |

Không mục nào thiếu task.

**Placeholder scan:** Task 12 Step 5 và Task 13 cố ý chờ số liệu thật — chúng không thể biết trước khi chạy. Mọi chỗ khác có code thật.

**Type consistency:**
- `AirflowClient.trigger_run(dag_id, conf)` khớp Task 3 và Task 4 ✔
- `AirflowClient.get_logs(dag_id, run_id, task_id, try_number)` khớp Task 3 và Task 5 ✔
- `RegistryClient.list_models()` / `.promote(name, version)` khớp Task 7 ✔
- `ReportsClient.latest(model_name)` / `.history(model_name, limit)` khớp Task 8 ✔
- `create_app(airflow, registry, reports, storage, probes)` — Task 2 tạo 4 tham số, Task 9 thêm `storage`, Task 11 nối bản thật. **Task 9 phải thêm `storage` vào chữ ký, không được quên** ✔ (đã ghi rõ ở Task 9 Step 3)

**Một điểm cần chú ý khi thực thi:** `create_app` lớn dần qua 4 task (2 → 4 → 7 → 8 → 9 → 11). Mỗi task chỉ thêm đúng phần của mình và **không được xoá wiring của task trước**. Task 11 là chỗ duy nhất đổi hành vi mặc định từ `None` sang client thật.
