"""Monitor stage - placeholder until Task 9 fills it in.

Task 1 needs the image to build so Evidently's real API can be verified
inside it. The Dockerfile copies this file, so it has to exist.
"""

print("ml-monitor image ready; main.py arrives in Task 9")
